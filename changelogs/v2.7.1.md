## What's new

### Fixed

- **Local servers that speak the Responses API are now detected.** Ollama, LM Studio, vLLM and llama.cpp builds with `/responses` enabled were previously assumed chat-completions-only on desktop; a local endpoint is now probed for `/responses` like any other provider and uses it when available.
