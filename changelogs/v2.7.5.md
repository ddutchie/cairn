## What's new

### Added

- **Choose a font for your note text.** Pick between Sans (the default), Serif, or Mono for note content — the editor, the preview, and PDF exports all use the chosen font. UI chrome stays on the system font. Set it in Settings → General ("Note font") or the quick-settings popover next to font size. System font stacks are used throughout so exports render identically on every platform.
- **Chat themes.** Re-skin the chat surface with full distinct looks — each theme bundles its own chat font, a background treatment (solid, gradient, or scanline pattern), a bubble style (filled, glass, or outlined), and a dark/light palette. Five built-in themes ship out of the box (Default, Paper, Terminal, Midnight, Aurora), and more arrive over time from the cairn-community catalog — no app update needed. Pick one in Settings → General ("Chat theme") or the quick-settings popover. Accent colour and note font are independent of the chat theme.
- **Richer chat themes.** Every theme now also sets its text weight (regular/medium), letter-spacing, and line-height, a named background pattern (scanlines, dots, grid, crosshatch, diagonal, or noise), a bubble corner radius (small, medium, or pill), and a bubble shadow — so community themes can go beyond the built-ins' looks. Gradients can use any number of colour stops, and AI-bubble text colour is now always explicit (no more washed-out fallback). The community catalog's themes (Ocean, Glacier, Matcha, Noir, Synthwave, Twilight, Ember, Forest) are updated to the richer shape.

### Fixed

- **Community chat themes now load on desktop.** The theme picker serves the cached community manifest instantly but then always refreshes it in the background — so a stale cache (e.g. one written before the theme shape expanded, whose entries fail the new validation and would otherwise render an empty catalog) self-heals, and newly-published community themes appear without a manual refresh.
- **Community chat themes now apply.** Previously the theme surface resolved community ids only inside the picker — a stored or selected community theme id silently fell back to the Default look because `applyChatTheme` never had the community catalog at hand (boot, dark/light flip, in-chat quick settings). The catalog is now fetched and cached at app boot and by every picker, and the active theme re-applies the moment a community id can resolve — so picking Ocean/Glacier/Noir etc. actually changes the chat surface everywhere.
- **Chat themes are in the in-chat quick-settings.** The Chat Settings popover (steps/temperature) now has a collapsible "Chat theme" swatch grid, matching the topbar quick-settings. Both quick-settings theme sections collapse under a chevron to keep the popovers compact now that the grid can show built-ins + community themes together.
- **Pattern backgrounds now render on mobile.** The scanline/pattern chat themes (Terminal, Ember, Matcha) previously fell back to a flat colour on iOS/Android — the pattern overlay is now drawn with the same named patterns as the desktop.

- **Deleting an automation now removes its folder from disk.** Previously the automation's row (and run history) were deleted from the database, but its on-disk folder (`<project>/.automations/<id>/` — scripts, `out/`, `.env`, manifest, and every run's transcript + scratch) was left behind. Deleting an automation now also removes the folder and purges its keychain env secrets, so nothing orphaned lingers after the automation is gone.
