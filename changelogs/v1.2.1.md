# v1.2.1

## What's new

### Features

- **Plan Mode** — the Cairn Agent can now be launched in Plan Mode. The agent reads your codebase (read-only tools only) and writes a PRD note rather than making changes. A mode badge appears in the chat header and an Approve Plan button promotes the session to Execute Mode, injecting the full PRD as context for the coding run. The plan note title is derived from the feature name (e.g. `Plan: Dark mode toggle`). The plan prompt now requires the agent to ground every step in actual code — citing exact file paths, function names, and line numbers — and to maintain a `## Tasks` checklist of independently-executable items.

- **`ask_questions` in Plan Mode** — the agent can now call `ask_questions` during planning to render a structured inline form in the chat UI (labelled fields, Cmd+Enter to submit). The plan mode system prompt directs the agent to prefer this over asking questions in prose. The `ask_questions` tool is also now available in Execute Mode for any interactive clarification needed mid-session.

- **Task archive management** — the Done column now has an "Archive all done" option in its `⋯` menu that bulk-archives every active card in one click. A new **Archive view** (Board / Archive toggle tab above the board) shows all archived tasks in a searchable grid with per-card Restore and Delete buttons, tooltips, click-to-open detail modal, and a "Delete all" button on the filter bar. Archived tasks are now included in all Insights canvases and the Knowledge Graph.

- **MCP archive tools** — two new MCP tools: `archive_task` (sets `archived_at`) and `restore_task` (clears it; queries the DB directly since archived cards are filtered from the snapshot). `list_tasks` gains an `includeArchived` parameter that returns archived cards grouped with column info and a restore hint.

- **Streaming test suite** — `electron/lib/pi-agent-loop.test.ts` — 8 mock scenarios (real Node HTTP server per test) covering: text-only, tool-call with no text, text-then-tool, multi-turn `onStepStart`, split SSE chunks, usage chunks, no-text tools-ready ordering, and parallel tool calls. Two live integration tests run against `TEST_LLM_BASE_URL` when `.env.test` is present (skipped otherwise).

- **`normaliseBaseUrl` helper** — strips trailing `/` and `/v1` from LLM endpoint URLs. Used in `pi-agent.ts`, `chat.ts`, `handlers.ts`, `AISettings.tsx`, and the test suite.

- **Incremental graph recompute** — `computeAutoRelationships` now accepts an optional `entityIds` array. `db:note:update` and `db:card:update` call the incremental variant, limiting recompute to affected nodes only. `GraphAIPanel.applyAction` collects affected IDs and triggers the same path.

- **AI visibility decoupled** — `aiEnabled` now controls only the inline AI suggestion buttons. View/nav visibility is controlled exclusively by `hiddenViews`. The two settings no longer affect each other.

---

### Fixes

- **Tool chips only appeared after stream completion** — root cause: the loop buffered all tool calls and only fired `onToolsReady` + `onToolStart` after `[DONE]`. Fixed by firing `onToolPending` during SSE streaming as soon as a tool name first appears in the delta, mirroring pi's approach of creating chips imperatively during `message_update`. The chip is now in the DOM within milliseconds of the LLM deciding to call a tool. `onToolStart` at execution time reuses the same `callId` to update the existing chip's label rather than creating a duplicate.

- **Tool chip spinner never showed (React 18 batching)** — even after the streaming fix, `onToolStart` and `onToolEnd` IPC messages arrived close enough together that React 18's automatic batching coalesced them into a single commit. Added `flushSync` around `addPiToolCall` to force a synchronous DOM commit before execution proceeds.

- **Tool chip race on IPC delivery** — `addPiToolCall` silently dropped chips when `onToolsReady` and `onToolStart` arrived in the same microtask batch (before `ensurePiStreamingMessage` had applied its state update). Fixed by making `addPiToolCall` atomically create the streaming message itself when none exists — same fix applied to `addPiSubagentToolCall`.

- **Main process blocking renderer IPC** — synchronous SQLite tool executions (Cairn DB writes) blocked the main process event loop between `onToolStart` and `onToolEnd` sends, causing both IPC messages to be delivered together. Added a `setImmediate` yield after `onToolStart` to flush the IPC queue before tool execution.

- **Parallel tool calls clobbering chip state** — `activeCallIds` in `PiAgentPane` was keyed by tool name. When the LLM emitted two calls to the same tool in one step (e.g. two `read` calls), the second `pending` event overwrote the first entry, causing the wrong chip to be resolved on `end`. Fixed by threading `callId` all the way through `onToolEnd` (loop → callback interface → `pi-agent.ts` → `subagent.ts`) and rekeying the renderer-side map by `callId` rather than tool name.

- **Archive all done — no re-render** — `getColumnCards` / `getArchivedColumnCards` are stable function references; `useShallow` never saw them change after bulk-archive. Added `cards: s.cards` to the board's shallow selector so any cards mutation triggers a re-render immediately.

- **Archive all done — optimistic rollback** — if the `db:cards:archive-done` IPC call fails, the optimistic Zustand update is now rolled back. The affected card IDs are snapshotted before the update and restored on error.

- **Archive all done — silent IPC error** — the `db:cards:archive-done` handler returned `{ error: "..." }` as its success value (wrapped inside `{ data: ... }` by `handle()`), so errors never surfaced. Changed to `throw new Error(...)` so `handle()` produces a top-level `{ error }` that triggers the toast.

- **"Archive all done" hidden when filter active** — visibility was gated on `filteredCards.length > 0`, so the menu item disappeared when a board filter was active. Changed to `allCards.length > 0` (pre-filter) since the action operates on the full column.

- **`list_tasks` `includeArchived` ignoring `columnType` filter** — the archived SQL query did not apply the `columnType` filter, returning cards from all column types regardless. Fixed by adding `AND bc.type = ?` to the query when `columnType` is set.

- **`archive_task` / `restore_task` returning "Unknown tool" in AI chat** — both tools were defined in MCP and `TOOL_SCHEMAS` but missing from `chat-executor.ts`. Added both cases; `restore_task` queries the DB directly since archived cards are excluded from the snapshot.

- **`onToolPending` missing from subagent callbacks** — `subagent.ts` constructed `AgentLoopCallbacks` without `onToolPending`, causing a TypeScript build error and no pending chips for subagent tools. Added the callback.

- **`onPlanNoteFound` firing unnecessarily in execute mode** — the `ensure_note` detection was already gated on `mode === "plan"` but without a comment. Made the intent explicit; the approve-plan `runAgentLoop` call also now receives `onPlanNoteFound` (was missing), ensuring the PRD chip stays in sync if the agent re-reads or re-saves the plan note.

- **Null task titles** — `create_task` in chat-executor, mcp-server, and the `db:card:create` handler now validate and trim titles. `spawn_tasks_from_note` skips blank-title items.

- **Blocker auto-clear on Done** — `db:card:update` (drag-and-drop path) now calls `clearBlockersFromAll` when `patch.columnId` resolves to a done-type column. Previously this only ran through MCP/chat-executor paths.

- **`pi-agent:tool` unknown status silently corrupting chip** — the `else` branch in `PiAgentPane` treated any unrecognised status as an `end` event. Changed to explicit `else if (e.status === "end")` with a `console.warn` for truly unknown statuses.

- **`PrdModal` done-detection fragile heuristic** — completion was detected by matching strings like "saved", "created", "prd" in the final assistant message with a length cap. Replaced with a reliable signal: done is set when a `create_note` or `ensure_note` tool call reaches `status === "done"` during the session.

- **Lint errors** — `handlers.ts`: escaped-quote parse error; `PiAgentPane`: `sendPromptRef.current` assigned during render (moved to `useLayoutEffect`); `GraphAIPanel`: complex expressions in `useMemo` dependency array (extracted to stable `nodeIds` / `edgeIds` primitives).

---

### Changes

- `pi-agent:tool` IPC event gains `status: "pending"` (fired during stream), `status: "end"` now carries `callId?`, and all three statuses include an optional `callId` field linking the chip lifecycle together.
- `AgentLoopCallbacks.onToolEnd` gains an optional `callId` parameter (5th arg). `onToolStart` already had it.
- `addPiToolCall` and `addPiSubagentToolCall` in the terminal-sessions store update an existing chip in-place when a matching `callId` is found, rather than appending a duplicate.
- `ask_questions` added to `CAIRN_TOOL_NAMES` and `PLAN_MODE_ALLOWED`; `pi-agent:ask-questions` IPC channel added to preload; `PiAgentPane` renders an inline `QuestionForm` when the agent calls it.
- Plan mode PRD format updated: `## Approach` requires file:line code references; `## Tasks` markdown checklist added; `## Affected Files` is now exhaustive; `## Background` requires actual code citations. The "how to behave" instructions now lead with codebase exploration before planning.
- `getArchivedProjectCards` selector added to `SelectorsSlice`.
- `GraphNode.meta` gains `isArchived?: boolean` — set for archived cards included in the graph.
- `loadGraph` card query no longer filters `archived_at IS NULL`; archived cards appear in `graphData.nodes` with `meta.isArchived: true`.
- `useScopedData` no longer filters `!c.archivedAt` from `scopedCards`; canvases that previously added inline `!c.archivedAt` guards (BulletCanvas, BeeswarmCanvas, SankeyCanvas) have those guards removed. BulletCanvas counts archived cards as done. SankeyCanvas routes archived cards to the `"done"` bucket.
- `.env.test` (gitignored) + `vitest.config.ts` dotenv loading for live test endpoint configuration (`TEST_LLM_BASE_URL`, `TEST_LLM_MODEL`, `TEST_LLM_API_KEY`).
- `~/.zshenv` created to run `path_helper` for all zsh instances, ensuring `node`/`npx` are on PATH in non-interactive shells.

---

## Changed files

| File | Change |
|------|--------|
| `electron/lib/pi-agent-loop.ts` | `onToolPending` callback; fire pending chips during SSE stream; `streamCallIds` map; `toolsReadyFired` flag; `setImmediate` yield before tool execution; `onToolStart` + `onToolEnd` gain optional `callId`; `ask_questions` emits `pi-agent:ask-questions` IPC; `ask_questions` in `CAIRN_TOOL_NAMES` + `PLAN_MODE_ALLOWED` |
| `electron/lib/pi-agent-loop.test.ts` | New — 8 SSE mock scenarios + 2 live integration tests; `.env.test` driven |
| `electron/lib/pi-agent-prompt.ts` | `buildPlanModePrompt`: explore-first instruction; `## Tasks` checklist; code-referenced `## Approach` and `## Background`; `ask_questions` in available tools; `buildExecuteModePrompt` unchanged |
| `electron/lib/coding-tools/subagent.ts` | `onToolPending` + `onToolEnd` `callId` wired |
| `electron/lib/llm.ts` | `normaliseBaseUrl` helper |
| `electron/ipc/pi-agent.ts` | `onToolPending` wired; `callId` on pending/start/end events; `approve-plan` IPC channel; `onPlanNoteFound` on approve-plan path; `mode` on prompt request |
| `electron/ipc/handlers.ts` | `db:card:update` calls incremental graph recompute + `clearBlockersFromAll` on Done; `db:card:create` validates title; `db:cards:archive-done` bulk-archive handler now throws on error |
| `electron/ipc/chat-executor.ts` | `create_task` + `spawn_tasks_from_note` validate title; `archive_task` + `restore_task` cases added |
| `electron/ipc/tool-parity.test.ts` | `archive_task` + `restore_task` added to documented tool set |
| `electron/mcp-server.ts` | `archive_task`, `restore_task` tools; `list_tasks` `includeArchived` + `columnType` filter fix; shape divergence documented |
| `electron/lib/tool-schemas.ts` | `archive_task`, `restore_task` schemas; `list_tasks` gains `includeArchived` |
| `electron/db/graph-queries.ts` | Card query removes `archived_at IS NULL`; adds `archived_at` to SELECT; sets `meta.isArchived` on node |
| `electron/preload.ts` | `card.archiveDone(columnId)`; `pi-agent:tool` status gains `"pending"` + `callId?`; `onAskQuestions` listener (`pi-agent:ask-questions`) |
| `electron/db/queries.ts` | — |
| `src/store/slices/terminal-sessions.ts` | `addPiToolCall` / `addPiSubagentToolCall` create streaming message atomically; update-in-place when `callId` matches; `archiveAllDoneCards` action |
| `src/store/slices/board.ts` | `archiveAllDoneCards(columnId)` — optimistic update + rollback on IPC failure |
| `src/store/slices/selectors.ts` | `getArchivedProjectCards` selector |
| `src/store/slices/ui.ts` | `aiEnabled` decoupled from `hiddenViews`; `toggleViewVisibility` closes chat panel |
| `src/store/slices/graph.ts` | `recomputeGraphRelationshipsIncremental` |
| `src/types/index.ts` | `GraphNode.meta` gains `isArchived?: boolean` |
| `src/components/agent/PiAgentPane.tsx` | `flushSync` on `addPiToolCall`; `status: "pending"` handling; `activeCallIds` rekeyed by `callId` not name; `pendingQuestions` state + `QuestionForm` render; `useLayoutEffect` for `sendPromptRef`; mode badge; Approve Plan button; PRD chip |
| `src/components/agent/SpawnAgentModal.tsx` | Plan/Execute toggle; `agentMode` state; `MapIcon` alias |
| `src/components/graph/analyticsHooks.ts` | `useScopedData` removes `!c.archivedAt` from `scopedCards` |
| `src/components/graph/BulletCanvas.tsx` | Archived cards count as done; removes inline `!c.archivedAt` guard |
| `src/components/graph/BeeswarmCanvas.tsx` | Removes inline `!c.archivedAt` guard |
| `src/components/graph/SankeyCanvas.tsx` | Archived cards route to `"done"` bucket; removes inline `!c.archivedAt` guard |
| `src/components/graph/GraphAIPanel.tsx` | `applyAction` collects affected IDs; calls incremental recompute; `useMemo` deps extracted to stable primitives |
| `src/components/kanban/board.tsx` | Archive view (Board/Archive toggle, searchable grid, restore/delete/detail); `archiveAllDoneCards` wired; `cards` in shallow selector; "Archive all done" visibility uses `allCards.length` |
| `src/components/kanban/column.tsx` | `onArchiveAllDone` prop; "Archive all done" dropdown item; visibility controlled by prop presence |
| `src/components/notes/notes-view/PrdModal.tsx` | Done detection replaced: tracks `create_note`/`ensure_note` tool completion instead of string-matching |
| `src/components/settings/AISettings.tsx` | `fetchModels` normalises baseUrl |
| `src/app/page.tsx` | — |
| `vitest.config.ts` | Loads `.env.test` via dotenv for live test endpoint |
| `.env.test` | New (gitignored) — `TEST_LLM_BASE_URL`, `TEST_LLM_MODEL`, `TEST_LLM_API_KEY` |
