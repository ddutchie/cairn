# v0.3.7

## Fixes

- **Tray icon missing in packaged app** — the production path for `trayTemplate.png` was pointing to `app.asar/public/` but Next.js copies `public/` assets into `out/` during the static export. Fixed the path to `app.asar/out/` so the tray icon resolves correctly in built app.
