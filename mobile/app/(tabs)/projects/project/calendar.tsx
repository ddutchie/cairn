import { ProjectCalendarScreen } from "@/screens/ProjectCalendarScreen";

// Projects-tab per-project calendar — nested so the tab bar stays visible.
export default function Screen() {
  return <ProjectCalendarScreen nested />;
}
