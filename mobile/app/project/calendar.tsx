import { ProjectCalendarScreen } from "@/screens/ProjectCalendarScreen";

// Root-stack per-project calendar (reached from the root project screen).
export default function Screen() {
  return <ProjectCalendarScreen />;
}
